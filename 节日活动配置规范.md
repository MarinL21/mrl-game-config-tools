# P2 节日活动配置规范文档

> 基于 Google Sheets 实际配置表整理，覆盖活动日历、任务系统、累充系统、礼包系统、行军表情/特效礼包、联动礼包六大模块。
> 
> 最后更新：2026-04-15

---

## 目录

1. [配置表总览](#1-配置表总览)
2. [activity_calendar_QA — 活动日历](#2-activity_calendar_qa--活动日历)
3. [activity_task_QA — 活动任务/奖励](#3-activity_task_qa--活动任务奖励)
4. [activity_rank_rule — 累充排行规则](#4-activity_rank_rule--累充排行规则)
5. [节日累充配置 — 礼包ID管理](#5-节日累充配置--礼包id管理)
6. [礼包表 — 商店礼包结构](#6-礼包表--商店礼包结构)
7. [行军表情礼包 / 行军特效礼包 / 联动礼包](#7-行军表情礼包--行军特效礼包--联动礼包)
8. [1525_lp_emoji — 表情资源表](#8-1525_lp_emoji--表情资源表)
9. [奖励 JSON 通用规范](#9-奖励-json-通用规范)
10. [跨节日换档操作规范](#10-跨节日换档操作规范)
11. [常见错误与校验规则](#11-常见错误与校验规则)

---

## 1. 配置表总览

| 表名 | Spreadsheet ID | 核心用途 |
|---|---|---|
| `2111_p2_activity_calendar` | `1OaExug4AwwFlGH6LGbBiMnvQF41hYg0LsXiMQZ9XX6g` | 每个活动的开关、时间、服务器范围 |
| `2111_p2_activity_task`（jiache） | `1K3-I4gCYKY-Zw5Ms05ozHtHKpOqYI-lp4kuuhqbWajY` | 活动任务条件 + 奖励配置 |
| `2122_p2_activity_rank_rule` | `1zziy6nMR1DlhCykKBndwk6d6KNRrzj1PsOsFGbLYR4M` | 累充/排行规则（计分、档位） |
| `节日累充配置` | `1RFAyBfpG3-8rm3ugNn3NHFdeDg8Erha0VttGzokIy6E` | 每节日礼包ID汇总、checklist |
| `X_行军特效直购礼包&联动礼包活动` | `1vo8xpPvYiwgBMWruBeOyD8wbez3rKOFulHCRqAWTgNk` | 行军特效/表情礼包数值设计模板 |
| `1525_lp_emoji` | `1GJBF_i1bZhl6l2gQ7b4lKU-XVT3xmeuvC88hoSiJhr8` | 表情资源底表（ID/组/展示开关） |
| `行军表情美术需求合集` | `1nftPsCQS9F5ppXA_VcTjIrwV4P_LGd6U9tCPdNnlBVA` | 每节日动态表情美术需求 |

---

## 2. activity_calendar_QA — 活动日历

### 2.1 字段说明

| 列名 | 类型 | 说明 | 示例 |
|---|---|---|---|
| `A_INT_id` | INT | 日历行唯一ID（自增） | `21115736` |
| `A_INT_activity_id` | INT | 活动ID，跨表关联核心字段 | `21127576` |
| `S_STR_comment` | STR | 备注，格式：`节日年份-活动名` | `科技节2026-行军表情礼包` |
| `S_MAP_server_info` | JSON | 投放服务器范围 | 见下方 |
| `S_MAP_start_trigger` | JSON | 活动开启触发器 | 见下方 |
| `S_MAP_time_info` | JSON | 活动时长信息 | 见下方 |
| `S_MAP_activity_group` | JSON | 活动分组（联动逻辑用） | `{}` 或 `{"group_id":xxx}` |
| `S_INT_data_cross` | INT | 跨服数据：0=不跨服，2=跨服 | `0` |
| `A_INT_country_use_type` | INT | 地区投放：0=全区，其他=特定区域 | `0` |

### 2.2 S_MAP_server_info 结构

```json
// 全体schema投放（最常用）
{"typ": "schema", "id": [1, 2, 3, 4, 5, 6]}

// 特定服务器投放
{"typ": "serverid", "id": [2012102, 3003202]}

// 新服生命周期（注册后N小时触发）
{"typ": "schema", "id": [0]}
```

### 2.3 S_MAP_start_trigger 结构

```json
// 时间触发（最常见的节日活动）
{"typ": "time", "is_ark": 1}

// 注册触发（新手活动）
{"typ": "reg"}

// 按UTC时间触发
{"typ": "afcutc", "val": "0h"}
```

### 2.4 S_MAP_time_info 结构

```json
// 节日活动（大多数）：空表示通过ARK统一控制时间
{}

// 固定时长活动（单位小时）
{"fcastdur": "0h", "actvdur": "288h", "closedur": "0h"}
```

| 字段 | 含义 |
|---|---|
| `fcastdur` | 预告时长 |
| `actvdur` | 活动持续时长 |
| `closedur` | 结束结算时长 |

### 2.5 行军特效/表情/联动礼包 活动ID 规律

节日活动三个礼包绑定在同一 group，按如下 comment 格式区分：

| comment格式 | 活动类型 |
|---|---|
| `XX节年份-行军表情礼包` | 行军表情礼包活动 |
| `XXXX年份-行军特效礼包` | 行军特效礼包活动 |
| `联动礼包-XXXX年份` | 联动礼包活动 |

**历史 activity_id 对照：**

| 节日 | 行军表情礼包 | 行军特效礼包 | 联动礼包 |
|---|---|---|---|
| 科技节2026 | `21127576` | `21127569` | `21127570` |
| 情人节2026 | `21127581` | `21127571` | `21127572` |
| 复活节2026 | `21127697` | `21127695` | `21127696` |

---

## 3. activity_task_QA — 活动任务/奖励

### 3.1 字段说明（共 18 列，A-R）

| 列 | 字段名 | 类型 | 说明 |
|---|---|---|---|
| A | `A_INT_group` | INT | 任务组ID，同一活动的任务共享同一 group |
| B | `A_INT_id` | INT | 任务唯一ID（11位整数） |
| C | `N_STR_comment` | STR | 备注，格式：`节日年份-任务类型-档位` |
| D | `A_MAP_showcond` | JSON | 显示条件（服务器开放时间、城堡等级等） |
| E | `A_MAP_fincond` | JSON | 完成条件（核心字段，见下方详解） |
| F | `A_INT_pretrace` | INT | 前置任务ID，0=无前置 |
| G | `A_ARR_reward` | JSON | 奖励数组（见第9节规范） |
| H | `A_STR_task_desc` | STR | 任务描述多语言 key（LC_EVENT_xxx） |
| I | `A_MAP_task_label_1` | JSON | 任务标签1（显示分类） |
| J | `A_MAP_task_label_2` | JSON | 任务标签2 |
| K | `A_INT_display_order` | INT | 显示排序（越大越靠前） |
| L | `A_INT_displaykey` | INT | 图标资源key |
| M | `A_MAP_filter` | JSON | 筛选条件（城堡等级过滤等） |
| N | `A_INT_daily_reset` | INT | 是否每日重置：0=否，1=是 |
| O | `A_STR_banner` | STR | banner资源key |
| P | `S_INT_redpoint_off` | INT | 红点关闭：0=开启红点 |
| Q | `A_INT_country_use_type` | INT | 地区类型，0=全区 |
| R | `A_INT_can_ad_reward` | INT | 广告奖励：0=否 |

### 3.2 A_MAP_fincond 完成条件详解

这是最核心的配置字段，决定任务的触发逻辑。

**累计充值条件（节日机甲累充常用）：**

```json
{
  "cat": 101412053,
  "arg": {
    "ids": [2011230006, 2011230236, ...]
  },
  "val": 1250,
  "op": "ge"
}
```

| 字段 | 说明 |
|---|---|
| `cat` | 条件类别：`101412053` = 累计充值金额（CD单位） |
| `arg.ids` | 统计哪些礼包ID的充值，来自`节日累充配置`表的C列 |
| `val` | 门槛值（CD单位，如1250 CD ≈ $1.25等效价值） |
| `op` | 比较算符：`ge` = 大于等于 |

**常用 cat 类别：**

| cat | 含义 |
|---|---|
| `101412053` | 累计充值（购买指定礼包总额，CD单位） |
| `10148026` | 累计登陆天数 |
| `10148028` | 活动积分累计（如弹珠点数） |
| `10145019/20` | 建设/战斗消耗 |

**A_MAP_fincond 典型档位（机甲累充为例）：**

| val | 约合美元 | 档位含义 |
|---|---|---|
| 1250 | $1.25 等效 | 最低档 |
| 3750 | $3.75 等效 | — |
| 12500 | $12.5 等效 | — |
| 25000 | $25 等效 | — |
| 50000 | $50 等效 | — |
| 125000 | $125 等效 | — |
| 250000 | $250 等效 | — |
| 625000 | $625 等效 | — |
| 1250000 | $1250 等效 | — |
| 2500000 | $2500 等效 | — |
| 5000000 | $5000 等效 | 最高档 |

### 3.3 A_MAP_showcond 显示条件

```json
// 活动开启后第0天至整个活动期间显示，城堡等级≥3
{
  "op": "and",
  "args": [
    {"op": "ge", "typ": "actvstarttime", "val": 0},
    {"op": "ge", "typ": "building", "id": 111811, "val": 3}
  ]
}

// 第3天至第4天显示（轮次控制）
{
  "op": "and",
  "args": [
    {"op": "ge", "typ": "actvstarttime", "val": 3},
    {"op": "lt", "typ": "actvstarttime", "val": 4},
    {"op": "ge", "typ": "building", "id": 111811, "val": 3}
  ]
}
```

| actvstarttime val | 含义 |
|---|---|
| 0 | 活动开始即显示 |
| N（整数） | 活动开始后第N天 |

---

## 4. activity_rank_rule — 累充排行规则

### 4.1 字段说明（共 17 列，A-Q）

| 列 | 字段名 | 类型 | 说明 |
|---|---|---|---|
| A | `A_INT_group` | INT | 规则组ID |
| B | `A_INT_id` | INT | 规则唯一ID（8位整数，如21222521） |
| C | `N_STR_comment` | STR | 备注，格式：`节日名称-规则说明` |
| D | `A_ARR_score_rule` | JSON | 计分规则数组（核心字段） |
| E | `A_MAP_start_time` | JSON | 起始时间类型 |
| F | `A_INT_rank_unit` | INT | 排行单位：1=个人，2=联盟 |
| G | `A_INT_rank_scope` | INT | 排行范围：1=服内，2=全球，3=联盟内 |
| H | `A_INT_rank_components` | INT | 排行组件类型 |
| I | `A_STR_section_lc` | STR | 段位LC key |
| J | `A_STR_rank_title` | STR | 排行榜标题LC key |
| K | `A_INT_icon_display_key` | INT | 图标展示key |
| L | `A_INT_min_score` | INT | 上榜最低分 |
| M | `A_ARR_rule_desc` | JSON | 规则描述 |
| N | `A_ARR_rule_extra` | JSON | 额外规则 |
| O | `A_INT_retain_rank` | INT | 保留排名数 |
| P | `A_MAP_score_req` | JSON | 积分要求 |
| Q | `A_INT_score_change_tips` | INT | 积分变化提示 |

### 4.2 A_ARR_score_rule 计分规则

```json
[
  {
    "cat": 10145019,
    "val": 1000,
    "score": 1
  },
  {
    "ids": [2011230006, 2011230236, ...],
    "val": 1000,
    "score": 1
  }
]
```

| 字段 | 说明 |
|---|---|
| `cat` | 按行为类别计分（不需要指定具体礼包） |
| `ids` | 按具体礼包ID列表计分（机甲累充使用） |
| `val` | 每N个单位算1分 |
| `score` | 得分值 |

**注意**：机甲累充中，包含 `ids` 字段的 score_rule（ID 21222521/21222523 类型），换节日时需要更新 `ids` 数组，来源同样是`节日累充配置`表的C列。

---

## 5. 节日累充配置 — 礼包ID管理

**文件**：`节日累充配置`（ID: `1RFAyBfpG3-8rm3ugNn3NHFdeDg8Erha0VttGzokIy6E`）

### 5.1 页签结构

| 页签命名 | 内容 |
|---|---|
| `礼包表` | 全量IAP礼包ID与描述（主数据源） |
| `汇总数据` | 历史各节日累充数据汇总 |
| `XX节checklist` | 本节日checklist（各活动是否已配置累充） |
| `XX节`（如`26科技节`） | 本节日参与累充的礼包ID列表（C列为关键列） |

### 5.2 节日页签（如 `26科技节`）数据结构

**第1行**：完整的 `arg.ids` 数组 JSON（可直接用于 fincond）

```json
[
  {"typ":"recharge_actv","id":21127573,"val":1},
  {"typ":"recharge_actv","id":21127577,"val":1},
  ...
]
```

**第2行**：copy格式（逗号分隔版，用于粘贴到配置工具）

**后续行（从第16行起）**：礼包行数据格式：

```
A列: 礼包外层ID（201xxxxx）
B列: 礼包类型（normal/random/metro_special 等）
C列: 礼包内层物品ID（2011xxxxx）← fincond的ids来源
D列: 渠道ID（如 2014001）
E列: 礼包描述（中文）
F列: LC文案key
```

**C列是核心**：`fincond.arg.ids` 和 `score_rule.ids` 中的ID列表，都来自此列（去重后使用）。

### 5.3 礼包表字段说明

| 字段 | 说明 |
|---|---|
| `A_INT_id` | 礼包外层ID（201xxxxx），商店展示用 |
| `N_STR_pkg_desc` | 礼包描述（如`节日挖矿连锁礼包_4.99`） |
| `A_STR_function` | 礼包功能类型（如`basic_CDs`/`normal`/`random`） |
| `A_STR_pkg_type` | 礼包类别 |
| `A_STR_paywall_tab` | 商店页签 |
| `A_BOL_pirce_display` | 是否显示价格 |
| `S_MAP_server_info` | 投放服务器范围 |
| `A_INT_priority` | 显示优先级（数字越小越靠前） |
| `A_MAP_time_info` | 时间信息 |
| `S_MAP_filters` | 过滤条件 |

### 5.4 checklist 格式

| 列 | 含义 |
|---|---|
| 负责人 | 配置人 |
| 节日活动名 | 活动类型 |
| 是否已贴累充表 | TRUE/FALSE |
| 是否上单笔充值 | TRUE/FALSE |
| 链接 | 配置文档链接 |

---

## 6. 礼包表 — 商店礼包结构

这里的"礼包"指前端商店展示的礼包，区别于 activity_task 里的奖励。

### 6.1 礼包类型分类

| `A_STR_function` | 中文说明 |
|---|---|
| `basic_CDs` | 基础CD礼包（常驻） |
| `normal` | 普通节日连锁礼包 |
| `random` | 随机礼包 |
| `metro_special` | 挖矿特权礼包 |
| `metro_normal` | 挖矿活动药水礼包 |
| `scene` | 场景类礼包（如矿洞解锁） |

---

## 7. 行军表情礼包 / 行军特效礼包 / 联动礼包

### 7.1 三类礼包定位

| 类别 | 业务定位 | 价格 | 限购 | 是否单独售卖 |
|---|---|---|---|---|
| 行军表情礼包（礼包B） | 低价单购，卖动态行军表情 | **$4.99** | 1次 | 是 |
| 行军特效礼包（礼包A） | 中高价单购，卖行军特效 | **$19.99** | 1次 | 是 |
| 联动礼包 | 双包购买后额外奖励 | 无单独价格 | — | 否 |

### 7.2 奖励构成标准模板

#### 礼包A — 行军特效礼包（$19.99）

```json
[
  {"asset":{"typ":"xp","id":11161002,"val":5000},"setting":{"serial_number":0,"ishighlight":false}},
  {"asset":{"typ":"item","id":"行军特效_item_id","val":1},"setting":{"serial_number":999,"ishighlight":true}},
  {"asset":{"typ":"item","id":"漫游骰子_item_id","val":20},"setting":{"serial_number":200,"ishighlight":true}},
  {"asset":{"typ":"item","id":"节日道具自选箱_item_id","val":10},"setting":{"serial_number":100,"ishighlight":true}},
  {"asset":{"typ":"item","id":"成长线自选宝箱_item_id","val":10},"setting":{"serial_number":10,"ishighlight":false}},
  {"asset":{"typ":"item","id":"联盟宝箱_item_id","val":1},"setting":{"serial_number":1,"ishighlight":false}}
]
```

**注**：XP（`typ:"xp"`）的 serial_number 为 0，排在所有 item 类型之后。

| 奖励项 | 数量 | serial_number | ishighlight |
|---|---|---|---|
| CD / XP | 5000 | 0 | false |
| VIP经验 | 5000 | 0 | false |
| 行军特效（主外显） | 1 | 999 | **true** |
| 漫游骰子（节日活动道具） | 20 | 200 | **true** |
| 节日道具自选箱 | 10 | 100 | **true** |
| 成长线自选宝箱 | 10 | 10 | false |
| 联盟宝箱 | 1 | 1 | false |

**ROI 参考**：整包 ROI ≈ 3.6；不含特效时 ROI ≈ 2.1

#### 礼包B — 行军表情礼包（$4.99）

| 奖励项 | 数量 | serial_number | ishighlight |
|---|---|---|---|
| CD / XP | 1250 | 0 | false |
| VIP经验 | 1250 | 0 | false |
| 动态行军表情（主外显） | 1 | 高位 | **true** |
| 漫游骰子 | 5 | 中位 | true/false |
| 节日道具自选箱 | 5 | 中位 | true/false |
| 成长线自选宝箱 | 2 | 低位 | false |
| 联盟宝箱 | 1 | 1 | false |

**ROI 参考**：整包 ROI ≈ 3.4；不含表情时 ROI ≈ 2.4

#### 联动礼包（购买礼包A+B后触发）

```json
[
  {"asset":{"typ":"item","id":"静态头像框_item_id","val":1},"setting":{"serial_number":5,"ishighlight":false}},
  {"asset":{"typ":"item","id":"漫游骰子_item_id","val":20},"setting":{"serial_number":5,"ishighlight":false}},
  {"asset":{"typ":"item","id":"节日道具自选箱_item_id","val":10},"setting":{"serial_number":5,"ishighlight":false}},
  {"asset":{"typ":"item","id":"成长线自选宝箱_item_id","val":5},"setting":{"serial_number":5,"ishighlight":false}}
]
```

| 奖励项 | 数量 |
|---|---|
| 静态头像框（主外显） | 1 |
| 漫游骰子 | 20 |
| 节日道具自选箱 | 10 |
| 成长线自选宝箱 | 5 |

**价值比例**：联动奖励占总价值 **26%**，礼包本体占 **74%**

### 7.3 行军特效 item_id 拆分规则

行军特效按**主题 × 时长**拆分为独立道具：

| 时长 | 说明 |
|---|---|
| 1天 | 试用版 |
| 3天 | 短期 |
| 7天 | 周 |
| 14天 | 双周 |
| 30天 | 月 |
| **永久** | **礼包内填写此版本** |

**配置要点**：礼包内的行军特效必须填写**永久版** item_id，其他时长版本用于其他场景（套餐分拆/活动奖励）。

### 7.4 行军特效礼包配置前置核查

在配置行军特效礼包之前，必须确认：

- [ ] 本季行军特效已有完整的6个时长版本 item_id（美术/程序已提供）
- [ ] 确认永久版 item_id（填入礼包奖励）
- [ ] 确认行军特效的属性定位（使用属性、图鉴属性）
- [ ] 活动界面 Banner 美术资源已就绪

### 7.5 联动礼包触发机制

联动礼包**不是**普通售卖包，触发逻辑为：

```
条件：已购买礼包A（行军特效礼包）AND 已购买礼包B（行军表情礼包）
结果：解锁联动奖励领取入口
```

配置侧的关联：联动礼包的 activity_id 需要绑定到同一节日的另外两个礼包的 activity_id 上，三个活动处于同一 group。

---

## 8. 1525_lp_emoji — 表情资源表

### 8.1 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| `Id` | INT | 单个表情资源ID（如 15250101） |
| `Comment` | STR | 表情名，格式：`角色名+序号_类型`（如`达西01_动态`） |
| `GroupId` | INT | 所属表情组（如 1525010） |
| `Show` | INT | 展示开关：0=不显示，1=显示 |
| `Distribution` | INT | 分发类型：0=系统默认，2=付费/礼包投放 |

### 8.2 静态 vs 动态表情区分规则

| 类型 | Show值 | Distribution值 | Comment后缀 | GroupId示例 |
|---|---|---|---|---|
| 静态表情 | 0 | 0 | `_静态` | 1525000x |
| 动态表情 | 1 | 2 | `_动态` | 1525010x |

### 8.3 配置注意事项

- 行军表情礼包卖的是**一组**表情资源，而非单个表情
- 必须确认 `GroupId`（整组解锁逻辑）
- 配置时要填 **表情道具的 item_id**（在物品表中），不是直接填 1525_lp_emoji 表里的 Id
- 1525_lp_emoji 的 Id 是底层资源，item_id 是游戏内道具（两层结构）

---

## 9. 奖励 JSON 通用规范

### 9.1 奖励数组结构

```json
[
  {
    "asset": {
      "typ": "item",      // "item" 或 "xp"
      "id": 11119294,     // 道具 item_id（整数）
      "val": 1            // 数量
    },
    "setting": {
      "serial_number": 999,     // 展示排序（越大越靠前；-1=特殊末位）
      "ishighlight": true       // 是否高亮展示
    }
  }
]
```

### 9.2 serial_number 排序规范

| 位置 | serial_number 范围 | 典型物品 | ishighlight |
|---|---|---|---|
| 主外显（卖点核心） | 999 | 行军特效、动态表情 | **true** |
| 核心活动道具 | 200 | 漫游骰子、节日道具 | true |
| 自选箱类 | 100 | 节日道具自选箱 | true |
| 成长线道具 | 10 | 成长线自选宝箱 | false |
| 通用附加项 | 1 | 联盟宝箱、帮派宝箱 | false |
| XP / CD | 0 或 -1 | XP（typ:"xp"） | false |

**规则**：
- `serial_number` 越大，在奖励列表中展示越靠前
- `ishighlight: true` = 奖励界面中重点高亮展示
- XP（`typ:"xp"`）的 serial_number 设为 0 或 -1，始终排在末尾
- 联动礼包奖励全部设为 `serial_number: 5`，无高亮层级区分

### 9.3 typ 类型

| typ | 含义 |
|---|---|
| `"item"` | 普通游戏道具（需提供 item_id） |
| `"xp"` | 经验值 / CD，固定 id 为 `11161002` |

### 9.4 固定道具 ID 参考

| 道具 | item_id | 说明 |
|---|---|---|
| XP / CD | `11161002` | typ 为 `"xp"` |
| 帮派宝箱（$0.99-4.99） | `11114316` | 固定配置 |
| 帮派宝箱（$9.99） | `11114317` | 固定配置 |
| 帮派宝箱（$19.99/$24.99） | `11114318` | 固定配置 |
| 帮派宝箱（$49.99） | `11114319` | 固定配置 |
| 帮派宝箱（$99.99） | `11114320` | 固定配置 |

### 9.5 掉落配置（drop）JSON 结构

```json
{
  "typ": "single_random",
  "num": 1,
  "args": [
    {
      "typ": "item",
      "id": 11112900,
      "num": 1,
      "wgt": 200
    },
    {
      "typ": "item",
      "id": 11112901,
      "num": 1,
      "wgt": 100
    }
  ]
}
```

| 字段 | 说明 |
|---|---|
| `typ` | 固定为 `"single_random"` |
| `num` | 每次从池子中抽取几个 |
| `args[].wgt` | 权重，掉落概率 = 该权重 / 总权重 |

---

## 10. 跨节日换档操作规范

### 10.1 每次必换（不可复用）

| 配置项 | 来源表 | 操作 |
|---|---|---|
| 三个活动的 `A_INT_activity_id` | activity_calendar_QA | 分配新ID，不复用旧ID |
| 行军表情 item_id + GroupId | 1525_lp_emoji / 物品表 | 确认本季表情道具ID |
| 行军特效 item_id（永久版） | 物品表 | 确认主题+永久版ID |
| 联动礼包主外显 item_id（头像框等） | 物品表 | 确认本季头像框ID |
| 节日活动道具 item_id（漫游骰子位置） | 物品表 | 替换为本季活动道具 |
| 节日道具自选箱 item_id | 物品表 | 替换为本季自选箱 |
| Banner 美术资源 | 美术交付 | 替换主图和角色资源 |
| `节日累充配置` 节日页签 C 列 | 节日累充配置 | 更新本节日参与累充的礼包ID列表 |
| `fincond.arg.ids` | activity_task_QA | 更新为新节日的礼包ID列表 |
| `score_rule.ids`（机甲累充） | activity_rank_rule | 更新为新节日的礼包ID列表 |

### 10.2 不用换（节日间保持不变）

| 配置项 | 固定值 | 说明 |
|---|---|---|
| 行军特效礼包价格 | $19.99 | 固定定价 |
| 行军表情礼包价格 | $4.99 | 固定定价 |
| 限购次数 | 1次 | 固定 |
| 联盟宝箱数量 | ×1 | 固定 |
| XP（CD）数量 | 5000（礼包A）/ 1250（礼包B） | 固定 |
| VIP经验数量 | 5000（礼包A）/ 1250（礼包B） | 固定 |
| 联动奖励/礼包本体价值比 | 26% / 74% | 固定 |
| 奖励 JSON 结构 | 见第9节 | 仅替换 id，结构不变 |
| serial_number 排序规则 | 见第9.2节 | 固定 |
| 联动礼包触发逻辑 | 双包购买解锁 | 固定 |

### 10.3 行军特效礼包换档 checklist

- [ ] 确认本季行军特效主题名称
- [ ] 确认6个时长版本 item_id 均已由程序提供
- [ ] 礼包奖励中填入**永久版** item_id
- [ ] 替换奖励 JSON 中的节日活动道具 item_id
- [ ] 替换奖励 JSON 中的节日自选箱 item_id
- [ ] 在 activity_calendar_QA 新增行，分配新 activity_id，comment 格式：`XX年份-行军特效礼包`
- [ ] 更新 Banner 美术资源

### 10.4 行军表情礼包换档 checklist

- [ ] 确认本季动态表情的 item_id 和 GroupId（查 1525_lp_emoji）
- [ ] 查 行军表情美术需求合集 对应节日页签，确认表情角色和内容
- [ ] 替换奖励 JSON 中的表情 item_id
- [ ] 替换奖励 JSON 中的节日活动道具 item_id
- [ ] 替换奖励 JSON 中的节日自选箱 item_id
- [ ] 在 activity_calendar_QA 新增行，comment 格式：`XX节年份-行军表情礼包`
- [ ] 更新 Banner（固定模板，替换角色主图和主色调）

### 10.5 联动礼包换档 checklist

- [ ] 确认本季联动奖励主外显 item_id（通常为静态头像框）
- [ ] 替换奖励 JSON 中的头像框 item_id
- [ ] 替换奖励 JSON 中的节日活动道具 item_id
- [ ] 替换奖励 JSON 中的节日自选箱 item_id
- [ ] 在 activity_calendar_QA 新增行，comment 格式：`联动礼包-XX年份`
- [ ] 确认联动触发条件中绑定的是**本节日**行军特效礼包和行军表情礼包的 activity_id

---

## 11. 常见错误与校验规则

### 11.1 高频错误

| 错误 | 原因 | 正确做法 |
|---|---|---|
| 联动礼包当成第三个直购包配置 | 概念混淆 | 联动礼包是双包触发后的奖励，无单独价格，不单独售卖 |
| 行军特效只写主题名，未落到具体 item_id | 缺少时长版本概念 | 必须落到**永久版**具体 item_id |
| 表情礼包只写表情名称，未确认 GroupId | 不了解底层表情结构 | 查 1525_lp_emoji 确认 GroupId |
| fincond ids 未更新为本节日礼包ID | 复制旧节日配置后漏改 | 每次换档必须从节日累充配置表重新同步 C 列 |
| serial_number 未区分主外显和补价值道具 | 不了解排序规则 | 主外显=999，核心道具=200，自选箱=100 |
| 奖励 JSON 中用了上一个节日的 item_id | 换档时只改了部分行 | 逐项核对所有节日相关 item_id |
| activity_id 复用旧节日 | 误以为可以复用 | 每个节日必须分配全新 activity_id |

### 11.2 配置校验规则

**activity_calendar_QA 校验：**
- `A_INT_activity_id` 在全表中必须唯一
- `S_STR_comment` 格式须包含节日名称和年份
- 同一节日的三个礼包活动（表情/特效/联动）必须归属同一 activity_group
- `S_MAP_server_info` 中的 schema id 不能为空数组

**activity_task_QA 校验：**
- `A_MAP_fincond` 必须是合法 JSON，cat/val/op 字段不能缺失
- 累充类任务（cat=101412053）的 `arg.ids` 不能为空数组
- `A_ARR_reward` 不能为空（联动礼包奖励至少4项）
- 同一活动的任务 `A_INT_group` 必须一致
- `A_INT_display_order` 同组内不能重复

**奖励 JSON 校验：**
- 每个 item 的 `id` 必须是有效的整数 item_id（非字符串）
- `serial_number` 不能全部相同（主外显需突出）
- 主外显道具（行军特效/表情）的 `ishighlight` 必须为 `true`
- `typ:"xp"` 的 `id` 固定为 `11161002`，不可修改

**行军特效礼包特有校验：**
- 填入的 item_id 必须是**永久版**特效，而非1天/7天等时限版
- 必须在配置前确认美术/程序已提供6个时长版本 item_id

**联动礼包特有校验：**
- 不应有单独的售卖价格
- 触发条件中引用的两个 activity_id 必须是**本节日**的行军表情礼包和行军特效礼包（不能跨节日混用）

---

## 附录：常用 ID 速查

### 帮派宝箱 ID（按价格档位）

| 价格 | item_id |
|---|---|
| ≤$4.99 | `11114316` |
| $9.99 | `11114317` |
| $19.99 / $24.99 | `11114318` |
| $49.99 | `11114319` |
| $99.99 | `11114320` |

### XP 相关

| 用途 | item_id/id |
|---|---|
| XP / CD（typ="xp"） | `11161002` |
| XP 计算公式 | 价格(USD) × 250，向上取整到50的倍数 |

### fincond cat 速查

| cat | 含义 |
|---|---|
| `101412053` | 节日累计充值（CD等效单位） |
| `10148026` | 累计登陆天数 |
| `10148028` | 活动积分累计 |
| `10145019` | 建设消耗 |
| `10145020` | 战斗消耗 |
