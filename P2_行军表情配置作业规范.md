# P2 行军表情配置作业规范

> 适用范围：以后所有“节日行军表情礼包”相关配置工作。
> 目标：避免再出现 ID 格式错误、跨表引用漏改、表头缺列、旧资源复用错误、测试页签位置错误、文本显示方式错误等问题。

---

## 1. 适用表范围

做一套完整的节日行军表情配置时，默认检查并处理以下表：

必做主链路：
1. `2111 activity_calendar_QA`
2. `2112 activity_config_qa`
3. `2121 activity_special_QA`
4. `2011 iap_config_QA`
5. `2013 iap_template_QA`
6. `1111 item`
7. `1180 qa`
8. `1168 get_access_group（杜绝手搓）`
9. `1511 display_key`

说明：
- `1525_lp_emoji` 当前不是这条链路的必配表，除非用户明确要求或项目规则变化，否则不要把 1525 当成必做项。

---

## 2. 创建测试页签规则

### 2.1 什么时候建测试页签

只有在用户明确说：
- "先做测试配置"
- "先放测试页"
- "我先检查你错误"
- 或其他明确要求先走测试页签

时，才建立独立测试页签。

默认规则：
- 正常配置直接写入 QA / 主页签
- 不额外建立测试页签

### 2.2 测试页签命名

测试页签统一命名：
- `2026拓荒节行军表情_测试_2111`
- `2026拓荒节行军表情_测试_2112`
- `2026拓荒节行军表情_测试_2121`
- `2026拓荒节行军表情_测试_2011`
- `2026拓荒节行军表情_测试_2013`
- `2026拓荒节行军表情_测试_1111`
- `2026拓荒节行军表情_测试_1180`
- `2026拓荒节行军表情_测试_1168`
- `2026拓荒节行军表情_测试_1511`

如换节日，替换“拓荒节”和年份即可。

### 2.3 测试页签位置

新建测试页签时：
- 不放最后
- 不放第一个
- 必须放在 QA / 主页签后面一个（index=1）

例如：
- `activity_config_qa` 后面紧跟测试页签
- `iap_template_QA` 后面紧跟测试页签
- `item` 后面紧跟测试页签
- `qa` 后面紧跟测试页签

### 2.4 文本显示格式

所有测试页签都统一设置：
- 文本换行策略 = `CLIP`
- 中文界面对应：`截短`
- 禁止使用溢出显示

---

## 3. 抄模板的原则

### 3.1 不要只对照一个模板

至少同时对照这三类信息：
1. 当前同功能最近节日模板
2. 当前目标节日同批次已有活动
3. 同年相邻节日的同类礼包

例如做“2026拓荒节行军表情礼包”时，至少同时看：
- 2025 拓荒节行军表情礼包
- 2026 科技节 / 情人节 / 复活节行军表情礼包
- 2026 拓荒节已有的同批活动（如行军特效、联动、头像框）

### 3.2 禁止直接复用旧节日资源

如果已经进入新年份或新节日：
- 不允许直接沿用上一年同节日表情道具 ID
- 不允许直接沿用别的节日表情道具 ID
- 必须先确认是否需要新建 `1111 + 1180 + 1168 + 1511` 资源链

---

## 4. ID 生成规则

### 4.1 基本原则

所有 `A_INT_id` / 主键 ID：
- 必须使用对应表的数字格式
- 必须全数字
- 必须不重复
- 必须先查该表现有最大值，再取新的可用值

示例：
- 2111 → `2111xxxx`
- 2112 → `2112xxxx`
- 2121 → `2121xxxx`
- 2011 → `2011xxxxxx`
- 2013 → `2013xxxxxx`
- 1111 → `1111xxxxx` 或当前表同段规则
- 1180 → `118000xx`
- 1168 → `1168xxxx`
- 1511 → `1511xxxxx`

### 4.1.1 2112 的特殊规则

`2112 activity_config_qa` 不能只看最大 ID 直接追加。
必须遵守：
- 新节日活动行要写到 `21128001 节日占位符（上面都是节日）` 这一行的上方
- 新 ID 从 `21129000` 开始顺延往下写
- 必须确保不重复
- 只要 2112 ID 改了，所有关联 2112 的地方必须同步改

本次已验证需要同步的位置至少包括：
- `2111.A_INT_activity_id`
- `2011.A_MAP_time_info.normal.actv_id`
- `1168.C_ARR_access_group.args[0]` 中挂的活动 ID
- 测试页 / QA 页中所有写死的 2112 引用

### 4.2 一个表多个新行时

如果一个表要新建多行：
- 连续取新的未使用 ID
- 不要混入占位字符串
- 不要使用 `NEW_xxx`

---

## 5. 表头同步规则

这是高频错误，必须严格执行。

### 5.1 先抄 QA 页签表头，再写内容

每个测试页签都必须：
1. 读取对应 QA / 主页签第 1 行表头
2. 原样复制全部表头到测试页签第 1 行
3. 按表头顺序逐列填写内容

### 5.2 禁止手写“简化版表头”

禁止为了省事只写前半段字段。
一旦少列，就会导致：
- 后续字段整体错位
- 数值看起来“像是对的”，实际写进了错误字段

### 5.3 本次曾漏过的典型字段

2013 常漏字段：
- `A_STR_banner_url`
- `A_MAP_special_style`
- `A_INT_country_use_type`
- `A_ARR_increment_items`

1111 常漏字段：
- `C_ARR_drop_display`
- `A_INT_source`
- `A_INT_get_access_group`
- `A_INT_country_use_type`

结论：
- 以后不做人工猜测
- 永远先把 QA 页签完整表头抄过来

---

## 6. 资源链路规则（1111 / 1180 / 1168 / 1511）

做“新节日行军表情”时，优先按下面链路处理：

### 6.1 1111 道具表

新表情道具行至少要包含：
- 新 item ID
- `A_STR_class = map_emoji`
- `C_INT_display_key = 新1511ID`
- `A_MAP_lc_name = LC_ITEM_map_emoji_xxx`
- `C_MAP_lc_desc = LC_ITEM_map_emoji_xxx_desc`
- `A_MAP_category_param.effect.map_emoji = 新1180ID`

### 6.2 1180 地图表情表

新表情展示行至少要包含：
- 新 1180 ID
- `A_STR_constant = map_emoji_xxx`
- `A_INT_emoji_type = 2`（动态）
- `C_INT_access_group = 新1168ID`
- `C_INT_display_key_emoji = 新1511ID`
- `A_INT_year_group = 当前年份`

位置规则：
- 正式写入 `1180 qa` 时，不是直接追加到最后
- 必须写到 `11800029` 这一行的下面
- 后续同类节日行军表情也按这个位置规则继续往下排

### 6.3 1168 get_access_group 表

`1180.C_INT_access_group` 不能拍脑袋写。
必须去 1168 表看结构，再新建或复用合法行。

本次确定的写法结构：
- `C_STR_item_label = non_item`
- `C_ARR_access_group` 中可挂活动入口，如：
  `[{"id":11531001,"args":["2112活动ID"]}]`

### 6.4 1511 display_key 表

`1180.C_INT_display_key_emoji` 和 `1111.C_INT_display_key` 不能乱填。
必须去 1511 表看结构，再新建或复用合法行。

本次确认：
- 1511 新行可作为表情展示 key 占位
- 新 1180 与新 1111 要同步引用同一个 1511 ID

### 6.5 同步原则

只要改了 `1180.C_INT_access_group` 或 `1180.C_INT_display_key_emoji`：
- 就要同步检查 `1111.C_INT_display_key`
- 就要同步检查 `1111.A_MAP_category_param.effect.map_emoji`
- 就要同步检查 `2121 emoji_show.A_INT_arg1`
- 就要同步检查 `2013` 奖励主外显 item ID

---

## 7. 2013 行军表情礼包配置规则

### 7.1 必查字段

每次做 2013 时，必须核对：
- `A_INT_CDs`
- `A_INT_all_value`
- `A_ARR_other_items`
- `A_STR_banner_url`
- `A_STR_pop_banner_url`
- `A_MAP_limit`
- `A_ARR_price_info`

### 7.2 不能乱抄别的节日值

要优先找：
- 同类型、同价位、同节日的历史版本
- 再参考同年相邻节日同价位礼包

### 7.3 本次确认过的 4.99 行军表情礼包关键值

若结构与拓荒节历史版一致，则：
- `A_INT_CDs = 1250`
- `A_INT_all_value = 6175`

### 7.4 Banner 相关

`A_STR_banner_url` 不要漏。
这列非常容易因为表头少列而错位。

本次对照后使用：
- `A_STR_banner_url = assets/operation/P2dlcimg/activityImg/EventBanner_BG_100.png`

---

## 8. 2011 配置规则

### 8.1 必须改的

- `A_INT_id`
- `N_STR_pkg_desc`
- `A_MAP_time_info.normal.actv_id = 新2112活动ID`

### 8.2 暂时不确定时的处理

如果 `A_ARR_iap_status` 对应的累充活动尚不明确：
- 不要乱填旧活动 ID
- 可以临时留空 `[]`
- 但要在备注里明确这是“待确认项”

---

## 9. 2121 配置规则

### 9.1 discount 行

必须检查：
- `A_STR_type = discount`
- `A_ARR_status = [{"typ":"iap","id":新2011ID}]`

### 9.2 emoji_show 行

必须检查：
- `A_STR_type = emoji_show`
- `A_INT_arg1 = 新1180ID`
- `A_MAP_expr.arg2 = 预览图路径`

注意：
- 这里填的是 1180 的表情资源 ID，不是 1111 item ID

---

## 10. 2112 配置规则

必须检查：
- `S_STR_comment`
- `A_STR_constant`
- `A_ARR_activity_components`
- `A_INT_show_hud`
- `S_STR_banner_url`
- `S_STR_calendar_banner_url`

### 10.1 components 引用

`A_ARR_activity_components` 必须和 2121 新行一致：
- discount -> 新2121 discount ID
- emoji_show -> 新2121 emoji_show ID

### 10.2 常量名

新节日必须用新常量：
- 不要沿用旧节日常量
- 命名要与现有活动风格一致

---

## 11. 2111 配置规则

必须检查：
- `A_INT_id`
- `A_INT_activity_id = 新2112ID`
- `S_STR_comment`
- 服务器范围
- 触发方式
- 时间配置

---

## 12. 提升到 QA 页签前的终检清单

在把测试页签内容提到 QA / 主页签之前，必须逐项打勾：

### 12.1 表结构
- [ ] 每个测试页签表头和 QA 页签完全一致
- [ ] 数据列数和表头列数一致
- [ ] 没有因缺列导致的字段错位

### 12.2 ID
- [ ] 所有主键 ID 都是纯数字
- [ ] ID 前缀符合表规则
- [ ] ID 未与现有行重复
- [ ] 2112 如是节日活动，已按 `21129000+` 规则分配

### 12.3 引用关系
- [ ] 2111 -> 2112 正确
- [ ] 2112 -> 2121 正确
- [ ] 2121 discount -> 2011 正确
- [ ] 2011 -> 2112 正确
- [ ] 2013 -> 2011 正确
- [ ] 1111 -> 1180 正确
- [ ] 1180 -> 1168 / 1511 正确

### 12.4 资源新旧
- [ ] 没有误用旧年份同节日资源
- [ ] 没有误用其他节日资源
- [ ] 新年份新节日如需新资源，已建立新 1111/1180/1168/1511 链

### 12.5 显示
- [ ] 测试页签位置在 QA 页签后面一个
- [ ] 文本换行策略为 CLIP（截短）

### 12.6 QA 提升
- [ ] 正常配置默认直接写 QA / 主页签
- [ ] 只有用户明确要求测试配置时才先建测试页
- [ ] 2112 已写到 `21128001 节日占位符` 上方
- [ ] 1180 已写到 `11800029` 下方
- [ ] 提升后再次回读确认 QA / 主页签最终落点内容无误

---

## 13. 标准作业顺序（SOP）

以后做同类配置，一律按这个顺序：

1. 查最近同类历史模板 + 当前目标节日同批活动
2. 判断本次是“直接上 QA”还是“用户明确要求先做测试页”
3. 为各表分配新 ID
4. 如需测试页，则新建测试页签并放在 QA 页签后面一个
5. 从 QA 页签复制完整表头
6. 填 2111 / 2112 / 2121 / 2011 / 2013 基础链路
7. 判断是否需要新建 1111 / 1180 / 1168 / 1511 资源链
8. 把资源链回填到 2121 / 2013 / 1111 / 1180
9. 全链路终检
10. 设置显示为“截短”
11. 正常配置直接写入 QA / 主页签：
   - 2112 写到 `21128001` 上方
   - 1180 写到 `11800029` 下方
   - 其他表按主表结构安全落位
12. 回读 QA / 主页签最终落点，再确认一次

---

## 14. 本次已验证的可复用经验

### 已验证不要再犯的错误
1. 测试页签位置不能放最后
2. 测试页签位置也不要放第一个，应放 QA 页签后面一个
3. 2013 和 1111 容易漏表头列，必须整行同步 QA 表头
4. 新年份节日表情不能直接沿用旧年的表情道具
5. 1180 的 `C_INT_access_group` 和 `C_INT_display_key_emoji` 必须去 1168 / 1511 查结构
6. 文本显示不能溢出，要统一设为截短

---

## 15. 推荐文件使用方式

以后再做这类工作时，先打开本文件，再按清单执行。

如果是 AI 执行，必须遵守：
- 先查结构，再落配置
- 先建测试页签，再提 QA
- 不确定的字段宁可标记待确认，也不要乱填旧值
